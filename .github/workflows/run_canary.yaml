## Canary CI/CD
name: Canary
run-name: Canary

on:
  push:
    branches:
      - master

jobs:
  #test:
    #uses: kaltura/playkit-js-common/.github/workflows/cypress.yml@master
    #secrets: inherit

  canary:
    if: ${{ github.actor != 'PlaykitJs-Bot' }}
    uses: kaltura/playkit-js-common/.github/workflows/canary_full_flow_plugin.yaml@365f676d8305f1b8f15d8174054e1d570639b3f5
    secrets: inherit
    with:
      schema-type: "playerV3OvpVersions"
      tests-yarn-run-to-execute: 'build'
      node-version: "18.x"

